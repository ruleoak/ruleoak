# ruleoak-protocol standalone archive pointer

The standalone `ruleoak-protocol` repository is archived for the 30-day Agent Action Control Plane sprint.

Protocol remains a real separately licensed package, but it now lives in the monorepo at:

```text
packages/protocol
```

Package name:

```text
@ruleoak/protocol
```

License:

```text
MIT
```
