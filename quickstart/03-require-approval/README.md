# Quickstart 03 — Require approval

```bash
npm run quickstart:03
```

This example pauses an external-send action and records an approval request. It demonstrates the developer value: risky actions can require human approval without putting policy rules inside the prompt.
