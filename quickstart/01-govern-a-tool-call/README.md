# Quickstart 01 — Govern one tool call

This is the smallest RuleOak pattern:

```bash
npm run quickstart:01
```

It declares one local retrieval tool, evaluates policy before execution, records evidence, and returns an `allowed` decision. This is the first thing a new developer should understand: RuleOak sits before the action boundary.
