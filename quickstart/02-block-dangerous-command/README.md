# Quickstart 02 — Block a dangerous command

```bash
npm run quickstart:02
```

The example evaluates a destructive workspace action and blocks it before execution. RuleOak does not execute the command; it creates a governance decision that a host application can enforce.
