{
  "serverInfo": { "name": "generic-workflow-mcp", "version": "0.1.0" },
  "tools": [
    { "name": "search_docs", "description": "Read-only document search", "inputSchema": { "type": "object", "properties": { "query": { "type": "string" } } } },
    { "name": "send_external_message", "description": "Send a message outside the local workspace", "inputSchema": { "type": "object", "properties": { "recipient": { "type": "string" }, "body": { "type": "string" } } } },
    { "name": "delete_workspace_file", "description": "Delete a local workspace file", "inputSchema": { "type": "object", "properties": { "path": { "type": "string" } } } }
  ],
  "resources": [
    { "uri": "file://workspace/docs", "name": "Workspace documentation" }
  ],
  "metadata": { "example": true, "boundary": "local-fixture-only" }
}
