# Telemetry Export Demo

RuleOak v1.5 can export local governance reports into OpenTelemetry-style JSON records.

Run:

```bash
npm run telemetry:export
```

The export is local-only and vendor-neutral. It does not send telemetry to any backend.
