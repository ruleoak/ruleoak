# CrewAI real-framework-ready example

Run:

```bash
npm run adapter:crewai:real
```

This example detects whether `crewai` is installed. It shows the same boundary you would use around a CrewAI tool: RuleOak decides before the tool executes.
