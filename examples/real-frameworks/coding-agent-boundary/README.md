# Coding agent boundary example

Run:

```bash
npm run adapter:coding-agent:real
```

This example is for developers building Claude Code / Codex-style custom coding agents. It shows how to let the agent read files and run safe tests while pausing repository writes and blocking destructive shell/file actions.
