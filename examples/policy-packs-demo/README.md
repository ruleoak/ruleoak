# Policy Packs Demo

This demo combines reusable RuleOak policy packs and applies them to governed tool calls.

Run:

```bash
npm run policy:demo
```

It demonstrates reusable policies for filesystem safety, external communication, ticketing writes, and cloud LLM approval.
