# RuleOak MCP Proxy Server Demo

Runs a local loopback JSON-RPC server that guards MCP-style `tools/call` requests before forwarding them to a fixture handler.

No external MCP server, credentials, or destructive tool execution are used.
