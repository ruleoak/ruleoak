# RuleOak Governance Record Schemas

These JSON Schema files document the RuleOak Governance Record Protocol v1. They are intentionally small and stable so TypeScript, Python, and future adapters can emit compatible records.

The conformance command validates the included golden records:

```bash
npm run protocol:conformance
```
